FROM microsoft/dotnet:1.0.0-preview2-sdk

COPY . /app

#Build lib project
WORKDIR /app/lib
RUN ["dotnet", "restore"]
RUN ["dotnet", "build"]

#Build the web project
WORKDIR /app/web
RUN ["dotnet", "restore"]
RUN ["dotnet", "build"]

ENTRYPOINT ["dotnet", "run"]